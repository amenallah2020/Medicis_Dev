@model IEnumerable<RHEVENT.Models.DA_LaboUser>

@{
    ViewBag.Title = "Create";

}

<h2>@ViewBag.userr</h2>

<br /><br />
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

      <div class="form-horizontal">
         
          <table class="table table-striped table-bordered " style="font-size:14px">


              <tr>
                  <th style="width:7%;">
                      <input type="checkbox" id="checkBoxAll" />  Tous
                  </th>
                  <th style="color:blue">
                      @Html.DisplayNameFor(model => model.Laboratoire)
                  </th>


              </tr>

              @if (Model != null)
              {
                  foreach (var item in Model)
                  {
              <tr>
                  @*@if (item.Etat == 1)
                  {
                      <td style="color:black; font-size:12px" align=center>
                          <input type="checkbox" name="Laboo" class="chkCheckBoxId" checked/>
                      </td>
                  }
                  else

                  {
              <td style="color:black; font-size:12px" align=center>
                  <input type="checkbox" name="Laboo" class="chkCheckBoxId" />
              </td>
                  }*@

                  <td style="color:black; font-size:12px">
                      @Html.DisplayFor(modelItem => item.Laboratoire)
                  </td>
              </tr>
                  }

              }
          </table>


          <div class="form-group">
              <div class=" col-md-10">
                  <input type="submit" value="Enregistrer" class="btn btn-success" />
              </div>
          </div>
      </div>
    }
            <div>
                @Html.ActionLink("Retour", "Index", "ApplicationUsers",null)
            </div>

            @section Scripts {
                @Scripts.Render("~/bundles/jqueryval")
            <script type="text/javascript">
        $(document).ready(function () {
            $('#checkBoxAll').click(function () {
                if ($(this).is(":checked", true))
                    $('.chkCheckBoxId').prop('checked', true);
                else
                    $('.chkCheckBoxId').prop('checked', false);
            });
        });
            </script>
            }
