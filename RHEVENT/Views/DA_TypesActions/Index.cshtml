@model IEnumerable<RHEVENT.Models.DA_TypesActions>

@{
    ViewBag.Title = "Index";
}

<h2>Types d'actions</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.TypeAction)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TypeActhat)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>


        <td>
            @Html.DisplayFor(modelItem => item.TypeAction)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TypeActhat)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
        </td>
    </tr>
}

</table>
