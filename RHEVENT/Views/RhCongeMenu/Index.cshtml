@{
        ViewBag.titre_page = "RH";
        ViewBag.email = ViewBag.email;
        ViewBag.nom_prenom = ViewBag.nom_prenom;
     
 }
  
<h2 class="text" style="color:coral">
    <span class="glyphicon   glyphicon-check"> </span>
    Congés
</h2>
<br /><br /> 
<div class="row">
    <p>
        <div class="col-lg-12">
            @*<span class="role user">*@
            <a class="btn btn-warning alert label-warning" role="button" href="@Url.Action("SoldeConge", "Conge" )">Solde congé</a>

            <a class="btn btn-warning alert label-warning" role="button" href="@Url.Action("Demander_conge", "Conge" )">Demande congé</a>

            <a class="btn btn-warning alert label-warning" role="button" href="@Url.Action("Mes_conges", "Conge" )"> Mes congés </a>

            @if ((User.IsInRole("RH") && User.IsInRole("RH_Admin")) || (User.IsInRole("RH") && User.IsInRole("RH_Directeur")) || (User.IsInRole("RH") && User.IsInRole("RH_Superieur_Hiéarchique")) || User.IsInRole("Admin_Medicis"))
            {
                <a class="btn btn-warning alert label-warning" role="button" href="@Url.Action("Approbation_superieur_hiéarchique", "Conge")">Approbation Sup.Hiérarchique </a>
            }

            </div>
    </p>

</div>