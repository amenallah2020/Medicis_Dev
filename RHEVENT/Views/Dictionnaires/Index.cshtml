@model IEnumerable<RHEVENT.Models.Dictionnaire>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.table)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.champ)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.valeur)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.signification)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.table)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.champ)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.valeur)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.signification)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
